import { WebSocketRouteIntegration, WebSocketRouteIntegrationBindOptions, WebSocketRouteIntegrationConfig } from '@aws-cdk/aws-apigatewayv2-alpha';
import { IFunction } from 'aws-cdk-lib/aws-lambda';
/**
 * (experimental) Lambda WebSocket Integration.
 *
 * @experimental
 */
export declare class WebSocketLambdaIntegration extends WebSocketRouteIntegration {
    private readonly handler;
    private readonly _id;
    /**
     * @param id id of the underlying integration construct.
     * @param handler the Lambda function handler.
     * @experimental
     */
    constructor(id: string, handler: IFunction);
    /**
     * (experimental) (experimental) Bind this integration to the route.
     *
     * @experimental
     */
    bind(options: WebSocketRouteIntegrationBindOptions): WebSocketRouteIntegrationConfig;
}
